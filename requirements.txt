# =============================================================================
# SAGE - Smart Autonomous General-purpose Engine
# Optimized for Raspberry Pi (ARM64)
# =============================================================================

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database & ORM
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9  # Pre-compiled for ARM

# Vector Storage
chromadb==0.4.22

# Data Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# LLM & AI
ollama==0.1.6
sentence-transformers==2.3.1

# Speech Processing (optimized for ARM/CPU)
faster-whisper==1.0.0  # Works on ARM, CPU-only mode
openai-whisper==20231117  # Fallback if faster-whisper has issues

# Text-to-Speech
# NOTE: piper-tts may require manual installation on some ARM systems
# Install via: pip install piper-tts or use precompiled binaries
TTS==0.22.0  # Alternative TTS with better ARM support

# Additional AI/ML dependencies
torch>=2.0.0  # PyTorch with CPU support for ARM
torchaudio>=2.0.0
transformers>=4.36.0
soundfile>=0.12.1
librosa>=0.10.0

# Utilities
python-dotenv==1.0.0
httpx==0.26.0
tenacity==8.2.3
aiofiles==23.2.1
pyyaml==6.0.1
